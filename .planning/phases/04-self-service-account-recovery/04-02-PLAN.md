---
phase: 04-self-service-account-recovery
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - backend/internal/handlers/auth.go
  - backend/internal/router/router.go
  - backend/internal/handlers/password_reset_test.go
autonomous: true
requirements:
  - AUTH-04
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - The backend exposes a reset-password completion endpoint that validates token state, updates the password, and consumes the token.
    - Reset tokens are rejected once used or expired.
    - The password reset path reuses shared password-validation rules instead of drifting away from the normal auth flow.
  artifacts:
    - backend/internal/handlers/auth.go
    - backend/internal/router/router.go
    - backend/internal/handlers/password_reset_test.go
  key_links:
    - `POST /api/auth/reset-password` completes recovery with a valid token and new password.
    - Successful reset updates the bcrypt password hash and forces the user back through the normal login flow rather than auto-login.
---

<objective>
Complete the backend consumption side of self-service account recovery.

Purpose: Creating reset tokens is not enough; users must be able to present a valid token, set a new password securely, and lose the ability to reuse the same link.
Output: Reset-password endpoint, shared password validation, and backend regression coverage for valid, used, and expired tokens.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-self-service-account-recovery/04-RESEARCH.md
@.planning/phases/04-self-service-account-recovery/04-01-PLAN.md
@backend/internal/handlers/auth.go
@backend/internal/router/router.go
@backend/internal/models/password_reset_token.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the reset-password completion endpoint and token-consumption rules</name>
  <files>backend/internal/handlers/auth.go, backend/internal/router/router.go</files>
  <read_first>backend/internal/handlers/auth.go, backend/internal/router/router.go, backend/internal/models/password_reset_token.go, .planning/phases/04-self-service-account-recovery/04-RESEARCH.md</read_first>
  <action>Add a dedicated reset-password handler in `backend/internal/handlers/auth.go` and wire it in `backend/internal/router/router.go`, for example at `POST /api/auth/reset-password`. The handler must accept a token plus password and password confirmation, validate token existence, reject expired or already-used tokens, update the stored password hash, and mark the reset token consumed. Do not automatically sign the user in after a successful reset.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/auth.go contains `ResetPassword`
    - backend/internal/router/router.go contains `/auth/reset-password`
    - backend/internal/handlers/auth.go contains `UsedAt` or equivalent token-consumption logic
  </acceptance_criteria>
  <done>Users can complete a password reset securely with a valid recovery token.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse shared password validation for registration and reset</name>
  <files>backend/internal/handlers/auth.go</files>
  <read_first>backend/internal/handlers/auth.go, backend/internal/handlers/password_reset_test.go</read_first>
  <action>Extract or add shared password validation logic in `backend/internal/handlers/auth.go` so registration and reset-password flows apply the same password rules. Keep the rules pragmatic for the existing app, but avoid allowing reset-specific validation to drift away from registration behavior. Reuse the existing bcrypt hashing path rather than redesigning password storage.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/auth.go contains `validatePassword` or equivalent shared password validation helper
    - backend/internal/handlers/auth.go uses the shared helper in both registration and reset-password paths
  </acceptance_criteria>
  <done>Password-reset completion stays aligned with the rest of the authentication system.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend tests for valid reset, expired tokens, used tokens, and login with the new password</name>
  <files>backend/internal/handlers/password_reset_test.go</files>
  <read_first>backend/internal/handlers/password_reset_test.go, backend/internal/handlers/auth.go, backend/internal/auth/jwt.go</read_first>
  <action>Extend `backend/internal/handlers/password_reset_test.go` to cover at least: successful reset with a valid token, reused-token rejection, expired-token rejection, and the ability to log in with the new password after a successful reset. Keep the tests focused on endpoint-level behavior rather than UI flow.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/password_reset_test.go contains `func TestResetPasswordAllowsLoginWithNewPassword`
    - backend/internal/handlers/password_reset_test.go contains `func TestResetPasswordRejectsUsedOrExpiredToken`
    - `cd backend && go test ./...` exits 0
  </acceptance_criteria>
  <done>The reset-consumption path is covered against the main lifecycle regressions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && go test ./...` succeeds
- [ ] `POST /api/auth/reset-password` accepts a valid token and rejects used/expired ones
- [ ] Successful reset updates the stored password hash
- [ ] User can log in through the normal login flow with the new password
</verification>

<success_criteria>

- All tasks completed
- Recovery tokens can be consumed securely exactly once
- Password reset uses the established password-hashing path
- Automated backend coverage exists for valid and invalid reset-token completion behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-self-service-account-recovery/04-02-SUMMARY.md`
</output>

