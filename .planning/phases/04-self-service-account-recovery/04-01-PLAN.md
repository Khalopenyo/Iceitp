---
phase: 04-self-service-account-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/password_reset_token.go
  - backend/internal/db/db.go
  - backend/internal/config/config.go
  - backend/internal/handlers/auth.go
  - backend/internal/router/router.go
  - backend/internal/mail/sender.go
  - backend/internal/handlers/password_reset_test.go
autonomous: true
requirements:
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - The backend has durable password-reset token state instead of a forgot-password stub with no recovery side effect.
    - Forgot-password requests always return a uniform outward response while only existing accounts receive reset side-channel delivery.
    - Reset URL construction and mail delivery use explicit trusted config rather than request-host inference.
  artifacts:
    - backend/internal/models/password_reset_token.go
    - backend/internal/handlers/auth.go
    - backend/internal/config/config.go
    - backend/internal/mail/sender.go
    - backend/internal/handlers/password_reset_test.go
  key_links:
    - `POST /api/auth/forgot-password` creates or rotates a recovery token for an existing account and returns a generic response for all requests.
    - Recovery email delivery is abstracted behind a sender that can be stubbed in tests and backed by SMTP in runtime config.
---

<objective>
Establish the backend request side of self-service account recovery.

Purpose: The current forgot-password endpoint is only a placeholder; users cannot recover access until the application can create secure recovery tokens and deliver reset links over email.
Output: Reset-token model, config/mailer foundation, real forgot-password request flow, and backend regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-self-service-account-recovery/04-RESEARCH.md
@backend/internal/handlers/auth.go
@backend/internal/config/config.go
@backend/internal/db/db.go
@backend/internal/auth/jwt.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add password-reset token persistence and trusted recovery config</name>
  <files>backend/internal/models/password_reset_token.go, backend/internal/db/db.go, backend/internal/config/config.go</files>
  <read_first>backend/internal/db/db.go, backend/internal/config/config.go, backend/internal/models/user.go, .planning/phases/04-self-service-account-recovery/04-RESEARCH.md</read_first>
  <action>Create a dedicated `PasswordResetToken` model in `backend/internal/models/password_reset_token.go` with user linkage, securely stored token material, expiry, single-use state, and request metadata. Add it to `backend/internal/db/db.go` migrations. Extend `backend/internal/config/config.go` with the trusted app-base-url and mail-delivery settings needed to construct reset links without relying on request-host data.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/models/password_reset_token.go contains `type PasswordResetToken struct`
    - backend/internal/models/password_reset_token.go contains `TokenHash`
    - backend/internal/models/password_reset_token.go contains `ExpiresAt`
    - backend/internal/db/db.go contains `&models.PasswordResetToken{}`
    - backend/internal/config/config.go contains `AppBaseURL` or equivalent trusted reset-url config
  </acceptance_criteria>
  <done>Recovery requests now have durable token state and trusted URL config instead of only a placeholder response.</done>
</task>

<task type="auto">
  <name>Task 2: Implement real forgot-password request handling and side-channel delivery</name>
  <files>backend/internal/handlers/auth.go, backend/internal/router/router.go, backend/internal/mail/sender.go</files>
  <read_first>backend/internal/handlers/auth.go, backend/internal/router/router.go, backend/internal/config/config.go, backend/internal/models/password_reset_token.go</read_first>
  <action>Replace the current forgot-password stub in `backend/internal/handlers/auth.go` with a real request flow that normalizes email input, preserves a uniform outward response, creates or rotates a secure reset token for an existing account, constructs a trusted reset URL from config, and sends that URL through a mail-sender abstraction in `backend/internal/mail/sender.go`. Keep the public message generic for both existing and non-existing accounts.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/auth.go contains `ForgotPassword`
    - backend/internal/handlers/auth.go contains `reset instructions sent if the email exists` or equivalent generic outward response
    - backend/internal/handlers/auth.go contains `PasswordResetToken`
    - backend/internal/mail/sender.go contains a sender interface or implementation for recovery email delivery
  </acceptance_criteria>
  <done>Forgot-password requests now trigger a real secure recovery side channel for existing accounts without exposing account existence.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend request-flow tests for uniform response and token creation behavior</name>
  <files>backend/internal/handlers/password_reset_test.go</files>
  <read_first>backend/internal/handlers/auth.go, backend/internal/models/password_reset_token.go, backend/internal/handlers/auth_test.go</read_first>
  <action>Create `backend/internal/handlers/password_reset_test.go` with coverage for the request side of recovery. Cover at least: existing and non-existing email addresses get the same outward response, an existing account receives a stored reset token, and tests can observe the side-channel sender through a stub rather than real SMTP.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/password_reset_test.go contains `func TestForgotPasswordReturnsUniformResponse`
    - backend/internal/handlers/password_reset_test.go contains `func TestForgotPasswordCreatesResetTokenForExistingUser`
    - `cd backend && go test ./...` exits 0
  </acceptance_criteria>
  <done>The request side of password recovery is protected by backend regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && go test ./...` succeeds
- [ ] `POST /api/auth/forgot-password` has a uniform outward response
- [ ] Existing accounts receive durable reset-token state and side-channel delivery
- [ ] Reset URLs are constructed from trusted config rather than request host data
</verification>

<success_criteria>

- All tasks completed
- Forgot-password is no longer a stub
- Recovery requests use secure token generation and real side-channel delivery
- Automated backend coverage exists for request-side enumeration resistance and token creation
</success_criteria>

<output>
After completion, create `.planning/phases/04-self-service-account-recovery/04-01-SUMMARY.md`
</output>

