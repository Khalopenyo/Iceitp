---
phase: 04-self-service-account-recovery
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - frontend/src/pages/Login.jsx
  - frontend/src/pages/ForgotPassword.jsx
  - frontend/src/pages/ResetPassword.jsx
  - frontend/src/App.jsx
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - Users can request a password reset and complete it from public recovery pages without logging in first.
    - The request page keeps outward messaging generic and does not leak whether the email exists.
    - The reset page requires password confirmation and returns the user to normal login on success.
  artifacts:
    - frontend/src/pages/ForgotPassword.jsx
    - frontend/src/pages/ResetPassword.jsx
    - frontend/src/pages/Login.jsx
    - frontend/src/App.jsx
  key_links:
    - Login page links into the recovery flow.
    - Public routes exist for both reset request and reset completion.
---

<objective>
Deliver the public recovery screens and login integration for self-service account recovery.

Purpose: Backend recovery endpoints are not user-accessible until the app provides request and reset pages that unauthenticated users can navigate.
Output: Forgot-password page, reset-password page, login integration, and route wiring.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-self-service-account-recovery/04-RESEARCH.md
@.planning/phases/04-self-service-account-recovery/04-02-PLAN.md
@frontend/src/pages/Login.jsx
@frontend/src/App.jsx
@frontend/src/lib/api.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the public forgot-password request page and route</name>
  <files>frontend/src/pages/ForgotPassword.jsx, frontend/src/App.jsx</files>
  <read_first>frontend/src/App.jsx, frontend/src/pages/Login.jsx, backend/internal/handlers/auth.go, .planning/phases/04-self-service-account-recovery/04-01-PLAN.md</read_first>
  <action>Create `frontend/src/pages/ForgotPassword.jsx` and wire it through `frontend/src/App.jsx` as a public route. The page should collect an email address, submit to `POST /api/auth/forgot-password`, and always show a generic success state compatible with the backend’s non-enumerating response.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/ForgotPassword.jsx contains `apiPost(\"/auth/forgot-password\"`
    - frontend/src/App.jsx contains `path=\"forgot-password\"`
    - frontend/src/pages/ForgotPassword.jsx contains generic success-state messaging rather than account-existence-specific copy
  </acceptance_criteria>
  <done>Users can request a reset email from a public recovery page.</done>
</task>

<task type="auto">
  <name>Task 2: Add the reset-password completion page with token and password confirmation handling</name>
  <files>frontend/src/pages/ResetPassword.jsx, frontend/src/App.jsx</files>
  <read_first>frontend/src/App.jsx, backend/internal/handlers/auth.go, .planning/phases/04-self-service-account-recovery/04-02-PLAN.md</read_first>
  <action>Create `frontend/src/pages/ResetPassword.jsx` and wire it in `frontend/src/App.jsx` as a public route. The page should read the reset token from the URL, require password confirmation, submit to `POST /api/auth/reset-password`, handle invalid/expired-token error feedback, and redirect the user back to `/login` after success instead of auto-login.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/ResetPassword.jsx contains `token`
    - frontend/src/pages/ResetPassword.jsx contains password confirmation handling
    - frontend/src/App.jsx contains `path=\"reset-password\"`
  </acceptance_criteria>
  <done>Users can complete a password reset from a public reset link page.</done>
</task>

<task type="auto">
  <name>Task 3: Link the login page into recovery flow without regressing normal auth</name>
  <files>frontend/src/pages/Login.jsx</files>
  <read_first>frontend/src/pages/Login.jsx, frontend/src/pages/ForgotPassword.jsx, frontend/src/pages/ResetPassword.jsx</read_first>
  <action>Update `frontend/src/pages/Login.jsx` to link clearly into the forgot-password flow while preserving the current login behavior. Keep the existing login submission path intact and make sure the recovery routes remain usable without authentication guards.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Login.jsx contains a link to `/forgot-password`
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>Recovery is discoverable from the login experience without breaking normal sign-in.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix frontend run build` succeeds
- [ ] Public recovery routes exist for request and reset
- [ ] Forgot-password UX keeps outward messaging generic
- [ ] Successful reset returns the user to normal login rather than automatic sign-in
</verification>

<success_criteria>

- All tasks completed
- Users can request and complete password recovery without organizer intervention
- Recovery pages are accessible before login
- Normal login remains intact after the recovery UX is added
</success_criteria>

<output>
After completion, create `.planning/phases/04-self-service-account-recovery/04-03-SUMMARY.md`
</output>

