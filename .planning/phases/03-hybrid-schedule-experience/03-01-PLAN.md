---
phase: 03-hybrid-schedule-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/handlers/program_readmodel.go
  - backend/internal/handlers/schedule.go
  - backend/internal/handlers/schedule_test.go
autonomous: true
requirements:
  - PROG-02
  - PART-01
  - PART-03
user_setup: []
must_haves:
  truths:
    - Participant-facing schedule endpoints use organizer-approved `ProgramAssignment` placement instead of raw profile section data.
    - The participant schedule contract exposes explicit pending versus approved state and includes room/join details appropriate to the approved attendance format.
    - The offline venue grouping endpoint is derived from authoritative room placement and can be consumed without room-name string heuristics.
  artifacts:
    - backend/internal/handlers/program_readmodel.go
    - backend/internal/handlers/schedule.go
    - backend/internal/handlers/schedule_test.go
  key_links:
    - `GET /api/schedule` returns authoritative participant schedule placement or an explicit pending state.
    - `GET /api/schedule/with-participants` returns authoritative room-oriented schedule data for the offline venue view.
---

<objective>
Establish the authoritative participant schedule contract for Phase 3.

Purpose: Hybrid participant UX cannot be correct until the participant-facing schedule endpoints read the same approved data already used by admins and program documents.
Output: Authoritative participant schedule projections, updated protected schedule endpoints, and backend regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hybrid-schedule-experience/03-RESEARCH.md
@backend/internal/handlers/program_readmodel.go
@backend/internal/handlers/schedule.go
@backend/internal/models/program_assignment.go
@backend/internal/models/room.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the authoritative read-model for participant-facing schedule views</name>
  <files>backend/internal/handlers/program_readmodel.go, backend/internal/handlers/schedule.go</files>
  <read_first>backend/internal/handlers/program_readmodel.go, backend/internal/handlers/schedule.go, backend/internal/models/program_assignment.go, backend/internal/models/room.go, .planning/phases/03-hybrid-schedule-experience/03-RESEARCH.md</read_first>
  <action>Extend `backend/internal/handlers/program_readmodel.go` with participant-facing projections that can represent one approved participant placement and room-oriented offline schedule groups. Use authoritative assignment data together with approved section and room details so later handler code can serve approved `talk_title`, `section_title`, `room_name`, `room_floor`, `starts_at`, `ends_at`, and `join_url` without falling back to raw profile schedule ownership.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/program_readmodel.go contains `type participantScheduleView struct`
    - backend/internal/handlers/program_readmodel.go contains `type roomScheduleGroup struct`
    - backend/internal/handlers/schedule.go contains a call into `loadParticipantScheduleView` or equivalent authoritative participant projection helper
  </acceptance_criteria>
  <done>Authoritative participant schedule data is available from shared backend projections rather than ad hoc handler logic.</done>
</task>

<task type="auto">
  <name>Task 2: Switch protected participant schedule endpoints to authoritative approved placement</name>
  <files>backend/internal/handlers/schedule.go</files>
  <read_first>backend/internal/handlers/schedule.go, backend/internal/handlers/program_readmodel.go, backend/internal/models/user.go, backend/internal/models/program_assignment.go</read_first>
  <action>Refactor `backend/internal/handlers/schedule.go` so `UserSchedule` returns an explicit `assignment_status` plus authoritative placement data, and `ParticipantSchedule` returns authoritative room/floor groupings for offline venue navigation. Remove raw schedule ownership from `user.Profile.SectionID` and `profiles.section_id` queries in these official participant readers. Preserve explicit pending behavior when no approved assignment exists.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/schedule.go contains `assignment_status`
    - backend/internal/handlers/schedule.go contains `current_user_type`
    - backend/internal/handlers/schedule.go does not contain `Where(\"profiles.section_id = ?\", section.ID)`
    - backend/internal/handlers/schedule.go does not contain a `UserSchedule` branch that uses `user.Profile.SectionID`
  </acceptance_criteria>
  <done>Participant schedule APIs now expose approved placement and explicit pending state rather than raw profile-derived schedule data.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for approved, pending, online, and offline participant schedule behavior</name>
  <files>backend/internal/handlers/schedule_test.go</files>
  <read_first>backend/internal/handlers/schedule.go, backend/internal/handlers/program_readmodel.go, backend/internal/handlers/program_test.go, backend/internal/handlers/auth_test.go</read_first>
  <action>Create `backend/internal/handlers/schedule_test.go` with focused coverage for participant-facing authoritative schedule behavior. Cover at least: approved offline placement includes room and floor details, approved online placement includes join-link access, and missing assignments return explicit pending state instead of raw-profile fallback. Include coverage for the room-oriented grouped response used by the venue page.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/schedule_test.go contains `func TestUserScheduleReturnsApprovedOfflinePlacement`
    - backend/internal/handlers/schedule_test.go contains `func TestUserScheduleReturnsApprovedOnlinePlacement`
    - backend/internal/handlers/schedule_test.go contains `func TestUserScheduleReturnsPendingWithoutAssignment`
    - `cd backend && go test ./...` exits 0
  </acceptance_criteria>
  <done>Hybrid participant schedule behavior is protected by backend regression coverage before frontend work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && go test ./...` succeeds
- [ ] `GET /api/schedule` returns authoritative approved placement or explicit pending state
- [ ] `GET /api/schedule/with-participants` groups offline venue data from approved room placement
- [ ] Protected participant schedule readers no longer depend on raw profile schedule ownership
</verification>

<success_criteria>

- All tasks completed
- Participant schedule APIs use the authoritative schedule source introduced in Phase 2
- Online/offline participant behavior is encoded in backend contract fields
- Automated backend coverage exists for approved and pending participant schedule states
</success_criteria>

<output>
After completion, create `.planning/phases/03-hybrid-schedule-experience/03-01-SUMMARY.md`
</output>

