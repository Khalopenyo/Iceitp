---
phase: 03-hybrid-schedule-experience
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - frontend/src/pages/Dashboard.jsx
autonomous: true
requirements:
  - PROG-02
  - PART-01
  - PART-03
user_setup: []
must_haves:
  truths:
    - The dashboard schedule tab renders the authoritative participant placement rather than raw profile section data.
    - Online and offline participants see different schedule affordances matched to the approved attendance format.
    - Participants without an approved assignment see an explicit pending message instead of unofficial schedule details.
  artifacts:
    - frontend/src/pages/Dashboard.jsx
  key_links:
    - Dashboard schedule UI consumes the updated `GET /api/schedule` contract from Phase 3 wave 1.
    - Online participants get a join-link-first schedule experience while offline participants get room/floor-oriented placement details.
---

<objective>
Deliver the role-aware participant schedule experience in the personal dashboard.

Purpose: The participant dashboard is the primary place where users confirm their final placement, so it must reflect approved schedule data and branch cleanly between online and offline attendance.
Output: Hybrid dashboard schedule UX built on the authoritative participant schedule API.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hybrid-schedule-experience/03-RESEARCH.md
@.planning/phases/03-hybrid-schedule-experience/03-01-PLAN.md
@frontend/src/pages/Dashboard.jsx
@frontend/src/lib/sessionStatus.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt dashboard schedule loading to the authoritative participant contract</name>
  <files>frontend/src/pages/Dashboard.jsx</files>
  <read_first>frontend/src/pages/Dashboard.jsx, backend/internal/handlers/schedule.go, .planning/phases/03-hybrid-schedule-experience/03-01-PLAN.md</read_first>
  <action>Refactor `frontend/src/pages/Dashboard.jsx` so the schedule tab reads the Phase 3 authoritative `/api/schedule` response instead of assuming `data.section` plus raw profile talk data. Preserve the rest of the dashboard tabs, but move the schedule tab onto explicit official-placement fields and pending-state handling from the updated backend contract.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Dashboard.jsx contains `assignment_status`
    - frontend/src/pages/Dashboard.jsx contains logic for an explicit pending/unapproved schedule state
    - frontend/src/pages/Dashboard.jsx does not contain `data.section.room` as the official schedule source
  </acceptance_criteria>
  <done>The dashboard schedule tab is bound to authoritative participant placement data instead of legacy section defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Render online and offline participant schedule states with the right calls to action</name>
  <files>frontend/src/pages/Dashboard.jsx</files>
  <read_first>frontend/src/pages/Dashboard.jsx, frontend/src/lib/sessionStatus.js, backend/internal/models/program_assignment.go</read_first>
  <action>Update the schedule tab in `frontend/src/pages/Dashboard.jsx` to branch on the approved attendance format. For approved online placement, show official section/time/talk details together with a visible external join-link action. For approved offline placement, show room and floor details together with venue-oriented cues. Keep status labels/time formatting coherent with the rest of the app.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Dashboard.jsx contains `join_url`
    - frontend/src/pages/Dashboard.jsx contains room and floor display logic for offline placement
    - frontend/src/pages/Dashboard.jsx contains online/offline conditional rendering keyed off approved user type or equivalent
  </acceptance_criteria>
  <done>Dashboard schedule UX now matches whether the participant attends online or offline.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve dashboard refresh and non-schedule flows while introducing hybrid schedule UX</name>
  <files>frontend/src/pages/Dashboard.jsx</files>
  <read_first>frontend/src/pages/Dashboard.jsx</read_first>
  <action>Keep profile editing, article checks, tab switching, and `/schedule` refresh behavior working after the hybrid schedule refactor. Ensure the schedule tab still updates after profile save and that the page builds cleanly without breaking the existing dashboard structure.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Dashboard.jsx still contains profile and materials tabs
    - frontend/src/pages/Dashboard.jsx still reloads `/schedule` after profile save or equivalent refresh path
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>Hybrid schedule UX is introduced without regressing the rest of the participant dashboard.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix frontend run build` succeeds
- [ ] Dashboard schedule tab shows approved placement or explicit pending state
- [ ] Online participants see join-link-first schedule UX
- [ ] Offline participants see room/floor placement details without breaking profile or article flows
</verification>

<success_criteria>

- All tasks completed
- Participants can confirm their final placement from the dashboard
- Online participants can reach the external meeting link directly from the schedule tab
- Offline participants can see official venue placement details from the same schedule tab
</success_criteria>

<output>
After completion, create `.planning/phases/03-hybrid-schedule-experience/03-02-SUMMARY.md`
</output>

