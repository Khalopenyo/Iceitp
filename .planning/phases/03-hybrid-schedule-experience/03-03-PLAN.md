---
phase: 03-hybrid-schedule-experience
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - frontend/src/pages/Map.jsx
  - frontend/src/components/Layout.jsx
autonomous: true
requirements:
  - PART-01
  - PART-02
  - PART-03
user_setup: []
must_haves:
  truths:
    - The venue map/list page uses authoritative approved room placement instead of matching legacy section-room strings.
    - Offline participants can browse sessions by official room/floor placement.
    - Online participants are no longer pushed into venue-specific map navigation from the main app shell.
  artifacts:
    - frontend/src/pages/Map.jsx
    - frontend/src/components/Layout.jsx
  key_links:
    - Venue navigation consumes the updated `GET /api/schedule/with-participants` contract from Phase 3 wave 1.
    - Main navigation only exposes the venue flow where it is relevant to the participant’s attendance mode.
---

<objective>
Finish the offline venue-navigation layer for the hybrid participant experience.

Purpose: Once the dashboard exposes approved placement, offline participants still need a room-oriented view for navigating the venue, while online participants should stop being steered toward irrelevant map flows.
Output: Authoritative offline venue page plus navigation gating for map access.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hybrid-schedule-experience/03-RESEARCH.md
@.planning/phases/03-hybrid-schedule-experience/03-01-PLAN.md
@frontend/src/pages/Map.jsx
@frontend/src/components/Layout.jsx
@frontend/src/data/rooms.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch the venue page to authoritative room-based grouping</name>
  <files>frontend/src/pages/Map.jsx</files>
  <read_first>frontend/src/pages/Map.jsx, backend/internal/handlers/schedule.go, .planning/phases/03-hybrid-schedule-experience/03-01-PLAN.md</read_first>
  <action>Refactor `frontend/src/pages/Map.jsx` to consume the updated authoritative `/api/schedule/with-participants` response and drive room selection from approved room identity/details rather than `section.room` string matching. Remove legacy room-heuristic helpers where they are no longer needed, and keep room/floor browsing focused on official placement data.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Map.jsx contains `apiGet(\"/schedule/with-participants\")`
    - frontend/src/pages/Map.jsx contains `room_id` or equivalent authoritative room identity field usage
    - frontend/src/pages/Map.jsx does not contain `roomMatchesSection`
  </acceptance_criteria>
  <done>The venue page uses authoritative room placement instead of reverse-engineering schedule ownership from strings.</done>
</task>

<task type="auto">
  <name>Task 2: Render an offline-friendly venue browsing experience from approved schedule data</name>
  <files>frontend/src/pages/Map.jsx</files>
  <read_first>frontend/src/pages/Map.jsx, frontend/src/lib/sessionStatus.js, frontend/src/data/rooms.js</read_first>
  <action>Update `frontend/src/pages/Map.jsx` so offline participants can browse approved sessions by room and floor, see official session timing, and identify their own placement in the room list. Keep the current room-card mental model if it still fits, but ensure empty states and selected-room details reflect authoritative schedule data rather than section defaults.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Map.jsx contains room/floor-specific session detail rendering from authoritative data
    - frontend/src/pages/Map.jsx highlights the current participant within room listings or equivalent own-placement affordance
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>Offline participants can navigate official venue placement through a room-oriented schedule experience.</done>
</task>

<task type="auto">
  <name>Task 3: Stop steering online participants into venue-specific map navigation</name>
  <files>frontend/src/components/Layout.jsx, frontend/src/pages/Map.jsx</files>
  <read_first>frontend/src/components/Layout.jsx, frontend/src/pages/Map.jsx, frontend/src/lib/auth.js</read_first>
  <action>Update `frontend/src/components/Layout.jsx` and, if needed, `frontend/src/pages/Map.jsx` so the `Карта` entry is only presented where it is relevant: offline participants and privileged admin/org roles. Online participants should either not see the map entry or receive an explicit non-venue state if they reach the page directly, keeping their primary schedule flow centered on the dashboard join link.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/components/Layout.jsx contains conditional map-navigation logic keyed by role and/or user type
    - frontend/src/pages/Map.jsx contains an explicit online-participant non-venue state or equivalent guard
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>Online participants are no longer pushed into offline venue navigation flows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix frontend run build` succeeds
- [ ] Venue page uses authoritative room placement rather than room-name string heuristics
- [ ] Offline participants can browse official room/floor schedule placement
- [ ] Online participants are not pushed into venue-specific map navigation
</verification>

<success_criteria>

- All tasks completed
- Offline participants can navigate official session placement by venue
- Venue browsing is derived from approved room assignments
- Online participants stay on a join-link-first path instead of venue navigation
</success_criteria>

<output>
After completion, create `.planning/phases/03-hybrid-schedule-experience/03-03-SUMMARY.md`
</output>

