---
phase: 06-participant-materials
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - frontend/src/pages/Documents.jsx
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
user_setup: []
must_haves:
  truths:
    - The participant document center shows what is available before the user clicks any action.
    - Online and offline participants see different material states where the product rules differ.
    - Participant feedback for document availability and failures is inline rather than alert-only.
  artifacts:
    - frontend/src/pages/Documents.jsx
  key_links:
    - `frontend/src/pages/Documents.jsx` consumes `GET /api/documents/status` before rendering material actions.
    - Material actions in the UI reflect the same availability state exposed by the backend contract.
---

<objective>
Turn the existing documents page into a real participant material center.

Purpose: The current page is only a set of buttons that fail late. Participants need a page that explains which materials are ready now, which are blocked, and which are not relevant to their attendance mode before they try to download anything.
Output: Status-aware documents UI with inline explanations, role-aware material states, and clean download/open actions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-participant-materials/06-RESEARCH.md
@frontend/src/pages/Documents.jsx
@backend/internal/handlers/documents.go
@frontend/src/lib/api.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load document readiness before rendering participant actions</name>
  <files>frontend/src/pages/Documents.jsx</files>
  <read_first>frontend/src/pages/Documents.jsx, backend/internal/handlers/documents.go</read_first>
  <action>Update `frontend/src/pages/Documents.jsx` so the page loads `GET /api/documents/status` on entry and keeps the response in local state before rendering material controls. Preserve the current download/open helpers where they still make sense, but stop relying on blind action clicks as the primary readiness mechanism.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Documents.jsx calls `/documents/status`
    - the page stores material availability state locally before rendering actions
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>The participant page can now explain material readiness using backend state rather than trial-and-error clicks.</done>
</task>

<task type="auto">
  <name>Task 2: Render explicit available, blocked, and not-applicable material states</name>
  <files>frontend/src/pages/Documents.jsx</files>
  <read_first>frontend/src/pages/Documents.jsx, .planning/phases/06-participant-materials/06-RESEARCH.md</read_first>
  <action>Refactor the document cards in `frontend/src/pages/Documents.jsx` so each material shows inline explanatory copy and a status-aware action state. Badge/certificate/proceedings should make attendance-mode or conference-state differences obvious, and blocked items should not depend on browser alerts to explain themselves.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Documents.jsx renders inline explanatory text for blocked materials
    - badge/certificate/proceedings cards display state derived from backend readiness data
    - the page distinguishes unavailable from not-applicable materials
  </acceptance_criteria>
  <done>Participants can understand why a document is or is not available before trying an action.</done>
</task>

<task type="auto">
  <name>Task 3: Replace alert-only failures with inline download/open feedback</name>
  <files>frontend/src/pages/Documents.jsx</files>
  <read_first>frontend/src/pages/Documents.jsx, frontend/src/lib/api.js</read_first>
  <action>Keep document download and proceedings-open actions simple, but replace alert-only error handling in `frontend/src/pages/Documents.jsx` with inline page feedback that survives the action attempt. Ensure successful paths still let the user download PDFs or open proceedings, while failures stay visible in the current document center.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Documents.jsx contains inline success or error state rendering for material actions
    - document-action failures are no longer communicated only through `alert(...)`
    - the page remains build-clean without touching unrelated global styles
  </acceptance_criteria>
  <done>The document center now behaves like a usable application page rather than a set of optimistic buttons.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix frontend run build` succeeds
- [ ] Participant sees readiness before clicking
- [ ] Badge/certificate/proceedings states are explicit and inline
- [ ] Document-action failures no longer rely on alert-only UX
</verification>

<success_criteria>

- All tasks completed
- The documents page is status-aware and role-aware
- Material actions show inline readiness and failure states
- Participant materials can be understood and used without trial-and-error clicking
</success_criteria>

<output>
After completion, create `.planning/phases/06-participant-materials/06-03-SUMMARY.md`
</output>
