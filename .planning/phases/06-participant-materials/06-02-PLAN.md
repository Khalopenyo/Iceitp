---
phase: 06-participant-materials
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - backend/internal/handlers/documents.go
  - backend/internal/handlers/documents_test.go
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
user_setup: []
must_haves:
  truths:
    - Download endpoints enforce the same attendance-aware rules shown by the material-status contract.
    - Badge, certificate, and proceedings behavior is explicit and regression-tested rather than relying on ad hoc handler checks.
    - Generated participant materials remain tied to authoritative conference data while exposing stable PDF/download behavior.
  artifacts:
    - backend/internal/handlers/documents.go
    - backend/internal/handlers/documents_test.go
  key_links:
    - `GET /api/documents/program`, `GET /api/documents/badge`, `GET /api/documents/certificate`, and `GET /api/documents/proceedings` share the same policy basis as `GET /api/documents/status`.
    - Certificate issuance and badge generation remain deterministic and bound to conference/user data already in the system.
---

<objective>
Harden material-download behavior against readiness and attendance rules.

Purpose: A status endpoint is not enough if the actual document handlers still follow looser or inconsistent rules. Participants need the real download/open actions to match the readiness contract exactly.
Output: Policy-aligned program/badge/certificate/proceedings handlers and backend tests for concrete download behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-participant-materials/06-RESEARCH.md
@backend/internal/handlers/documents.go
@backend/internal/handlers/checkin.go
@backend/internal/models/certificate.go
@backend/internal/models/checkin.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route all document handlers through the centralized material policy</name>
  <files>backend/internal/handlers/documents.go</files>
  <read_first>backend/internal/handlers/documents.go, .planning/phases/06-participant-materials/06-01-PLAN.md</read_first>
  <action>Refactor `ProgramPDF`, `BadgePDF`, `CertificatePDF`, and `Proceedings` in `backend/internal/handlers/documents.go` so each action consults the same centralized material-status/policy logic introduced in `06-01` before returning PDFs or URLs. Keep error messages clear and participant-facing, and eliminate duplicated ad hoc availability checks where possible.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - document handlers reuse a shared availability/policy path
    - badge and certificate downloads no longer rely on unrelated implicit assumptions
    - proceedings access and program empty-state behavior stay consistent with the status contract
  </acceptance_criteria>
  <done>The actual download/open endpoints now behave the same way the readiness API describes.</done>
</task>

<task type="auto">
  <name>Task 2: Make badge and certificate issuance explicitly attendance-aware</name>
  <files>backend/internal/handlers/documents.go</files>
  <read_first>backend/internal/handlers/documents.go, backend/internal/models/user.go, backend/internal/models/checkin.go, .planning/phases/06-participant-materials/06-RESEARCH.md</read_first>
  <action>Codify the v1 attendance-aware material rules recommended in `06-RESEARCH.md`: badge generation should be offline-only, while certificate eligibility should be explicit for both attendance modes instead of silently treating every participant as onsite-only. Keep the rule implementation small and grounded in existing evidence already stored by the system.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/documents.go contains explicit offline-only badge gating
    - backend/internal/handlers/documents.go contains explicit certificate eligibility logic by attendance mode
    - certificate numbering remains deterministic and stable for repeated downloads
  </acceptance_criteria>
  <done>Document issuance is now aligned with the conference’s online/offline participant model instead of one-size-fits-all behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Extend backend coverage to actual material download behavior</name>
  <files>backend/internal/handlers/documents_test.go</files>
  <read_first>backend/internal/handlers/documents_test.go, backend/internal/handlers/documents.go, backend/internal/handlers/program_readmodel_test.go</read_first>
  <action>Extend `backend/internal/handlers/documents_test.go` beyond status-only assertions so it covers real endpoint behavior for badge, certificate, program, and proceedings access. Include at least one regression case where the status contract and the concrete handler response must match for a blocked material.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/documents_test.go contains blocked badge/certificate/proceedings endpoint coverage
    - backend/internal/handlers/documents_test.go contains at least one successful PDF/download-path case
    - `cd backend && go test ./...` exits 0
  </acceptance_criteria>
  <done>Material download behavior is protected against policy regressions, not just status-contract drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && go test ./...` succeeds
- [ ] Download handlers enforce the same readiness rules exposed by `GET /api/documents/status`
- [ ] Badge eligibility is offline-only
- [ ] Certificate eligibility is explicit and attendance-aware
</verification>

<success_criteria>

- All tasks completed
- Material download handlers and status contract stay in sync
- Attendance-aware badge/certificate rules are explicit
- Automated backend coverage exists for concrete document-access behavior
</success_criteria>

<output>
After completion, create `.planning/phases/06-participant-materials/06-02-SUMMARY.md`
</output>
