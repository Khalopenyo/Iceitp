---
phase: 06-participant-materials
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/handlers/documents.go
  - backend/internal/router/router.go
  - backend/internal/handlers/documents_test.go
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
user_setup: []
must_haves:
  truths:
    - The backend exposes an explicit participant-material availability contract instead of forcing the UI to discover failures by clicking download buttons.
    - Material readiness is derived from authoritative schedule state, attendance mode, conference status, proceedings publication, and check-in evidence.
    - Backend regression coverage exists before the participant document-center UI depends on the new contract.
  artifacts:
    - backend/internal/handlers/documents.go
    - backend/internal/router/router.go
    - backend/internal/handlers/documents_test.go
  key_links:
    - `GET /api/documents/status` returns participant-usable availability for personal program, full program, badge, certificate, and proceedings.
    - The status contract uses the same centralized policy inputs that later document-download handlers will enforce.
---

<objective>
Establish the backend contract for participant materials.

Purpose: The platform already has raw document endpoints, but participants and frontend code cannot reliably know what is available or why a material is blocked. Phase 6 needs a stable status/readiness source before polishing downloads or participant UX.
Output: Centralized document-availability backend logic, authenticated status endpoint, and regression coverage for material readiness.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-participant-materials/06-RESEARCH.md
@backend/internal/handlers/documents.go
@backend/internal/handlers/program_readmodel.go
@backend/internal/handlers/checkin.go
@backend/internal/router/router.go
@frontend/src/pages/Documents.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a centralized participant-material status view</name>
  <files>backend/internal/handlers/documents.go</files>
  <read_first>backend/internal/handlers/documents.go, backend/internal/handlers/program_readmodel.go, backend/internal/models/conference.go, .planning/phases/06-participant-materials/06-RESEARCH.md</read_first>
  <action>Refactor `backend/internal/handlers/documents.go` around a shared participant-material status/readiness helper that evaluates personal program availability, full program availability, badge applicability, certificate eligibility, and proceedings readiness from authoritative assignment state, attendance mode, conference status, and check-in data. Keep the helper local to the existing document handler surface instead of creating a parallel materials subsystem.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/documents.go contains a document status/readiness response type
    - backend/internal/handlers/documents.go contains explicit badge/certificate/proceedings availability logic
    - program availability is derived from authoritative assignment state rather than raw profile assumptions
  </acceptance_criteria>
  <done>The backend can now answer “what materials are available and why?” from one authoritative place.</done>
</task>

<task type="auto">
  <name>Task 2: Expose an authenticated materials-status endpoint for participant UI</name>
  <files>backend/internal/handlers/documents.go, backend/internal/router/router.go</files>
  <read_first>backend/internal/router/router.go, backend/internal/handlers/documents.go, frontend/src/pages/Documents.jsx</read_first>
  <action>Add an authenticated `GET /api/documents/status` endpoint that returns participant-usable availability and explanatory messages for each document/material. Keep the payload focused on the current participant’s document center and reuse the centralized helper from Task 1 instead of duplicating rule branches in the handler.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/documents.go contains `DocumentStatus` or equivalent handler
    - backend/internal/router/router.go contains `protected.GET(\"/documents/status\"`
    - the response includes explicit availability state for badge, certificate, and proceedings
  </acceptance_criteria>
  <done>The frontend has a stable API contract for material readiness instead of inferring behavior from PDF failures.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend regression tests for participant-material readiness states</name>
  <files>backend/internal/handlers/documents_test.go</files>
  <read_first>backend/internal/handlers/documents.go, backend/internal/handlers/chat_test.go, backend/internal/models/certificate.go, backend/internal/models/checkin.go</read_first>
  <action>Create `backend/internal/handlers/documents_test.go` with endpoint-level coverage for the new status contract. Cover at least: personal program pending state, offline-vs-online badge availability, certificate readiness behavior, and proceedings availability based on conference status plus configured URL.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/documents_test.go contains `func TestDocumentStatusReflectsProgramAndProceedingsState`
    - backend/internal/handlers/documents_test.go contains badge availability coverage for online vs offline participants
    - `cd backend && go test ./...` exits 0
  </acceptance_criteria>
  <done>Participant-material readiness is protected by backend regression coverage before the UI depends on it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && go test ./...` succeeds
- [ ] `GET /api/documents/status` returns stable participant-material availability data
- [ ] Status output distinguishes available, blocked, and not-applicable material states
- [ ] Badge, certificate, and proceedings readiness are derived from explicit policy inputs rather than implicit handler fallthrough
</verification>

<success_criteria>

- All tasks completed
- Participant materials have a stable backend readiness contract
- Backend regression coverage exists for material availability
- The frontend no longer needs to guess document readiness from PDF/download failures alone
</success_criteria>

<output>
After completion, create `.planning/phases/06-participant-materials/06-01-SUMMARY.md`
</output>
