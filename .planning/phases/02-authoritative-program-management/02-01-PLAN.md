---
phase: 02-authoritative-program-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/program_assignment.go
  - backend/internal/db/db.go
  - backend/internal/handlers/program.go
  - backend/internal/router/router.go
  - backend/cmd/server/main.go
  - backend/internal/handlers/program_test.go
autonomous: true
requirements:
  - PROF-04
  - PROG-01
  - PROG-03
  - PROG-04
user_setup: []
must_haves:
  truths:
    - The backend has a dedicated authoritative program-assignment model instead of treating participant profile data as the official program source.
    - Admin can load and upsert approved attendance format, section, talk title, room, slot, and join-link data through a dedicated API.
    - Server restart no longer deletes non-preset rooms or rewrites section room assignments during normal startup.
  artifacts:
    - backend/internal/models/program_assignment.go
    - backend/internal/handlers/program.go
    - backend/internal/router/router.go
    - backend/cmd/server/main.go
    - backend/internal/handlers/program_test.go
  key_links:
    - GET /api/admin/program returns participant registration inputs plus authoritative assignment data.
    - PUT /api/admin/program/:userID creates or updates the authoritative assignment used by later admin/program readers.
---

<objective>
Establish the authoritative program-management backend contract for Phase 2.

Purpose: Admin UI and official program consumers need a stable, durable source of truth before organizer-controlled scheduling can be built.
Output: New authoritative program-assignment model, admin API endpoints, safe startup behavior, and backend regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authoritative-program-management/02-RESEARCH.md
@backend/internal/models/user.go
@backend/internal/models/section.go
@backend/internal/models/room.go
@backend/internal/db/db.go
@backend/internal/router/router.go
@backend/cmd/server/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the authoritative program-assignment model and migration</name>
  <files>backend/internal/models/program_assignment.go, backend/internal/db/db.go</files>
  <read_first>backend/internal/models/user.go, backend/internal/models/section.go, backend/internal/models/room.go, backend/internal/db/db.go, .planning/phases/02-authoritative-program-management/02-RESEARCH.md</read_first>
  <action>Create a new `ProgramAssignment` model in `backend/internal/models/program_assignment.go` that stores one authoritative admin-approved record per participant talk with at least `user_id`, `user_type`, `section_id`, `talk_title`, `room_id`, `starts_at`, `ends_at`, and `join_url`. Add the model to `backend/internal/db/db.go` migrations and define relations/constraints so later read-model code can join participants, sections, and rooms cleanly.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/models/program_assignment.go contains `type ProgramAssignment struct`
    - backend/internal/models/program_assignment.go contains `UserType`
    - backend/internal/models/program_assignment.go contains `JoinURL`
    - backend/internal/db/db.go contains `&models.ProgramAssignment{}`
  </acceptance_criteria>
  <done>The backend has a dedicated authoritative assignment entity ready for organizer-managed program data.</done>
</task>

<task type="auto">
  <name>Task 2: Expose admin program-management APIs with authoritative validation rules</name>
  <files>backend/internal/handlers/program.go, backend/internal/router/router.go, backend/internal/handlers/program_test.go</files>
  <read_first>backend/internal/router/router.go, backend/internal/handlers/users.go, backend/internal/handlers/schedule.go, backend/internal/models/program_assignment.go, backend/internal/models/user.go, backend/internal/models/section.go, backend/internal/models/room.go</read_first>
  <action>Add a dedicated program-management handler in `backend/internal/handlers/program.go` and wire it in `backend/internal/router/router.go`. Provide `GET /api/admin/program` for organizer program data loading and `PUT /api/admin/program/:userID` for upserting one authoritative assignment. Validate user existence, allowed `user_type`, optional section and room references, `starts_at < ends_at`, and safe `http/https` join-link formats. Create `backend/internal/handlers/program_test.go` to cover successful upsert, invalid room/section/user errors, and join-link validation.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/router/router.go contains `/admin/program`
    - backend/internal/handlers/program.go contains `GET /api/admin/program` behavior or handler equivalent
    - backend/internal/handlers/program.go contains `join_url`
    - backend/internal/handlers/program_test.go contains `func TestUpsertProgramAssignment`
    - backend/internal/handlers/program_test.go contains `func TestUpsertProgramAssignmentRejectsInvalidJoinURL`
  </acceptance_criteria>
  <done>The admin frontend has a stable authoritative backend API for approved program data.</done>
</task>

<task type="auto">
  <name>Task 3: Remove destructive startup behavior that would overwrite authoritative program data</name>
  <files>backend/cmd/server/main.go</files>
  <read_first>backend/cmd/server/main.go, backend/internal/handlers/rooms.go, backend/internal/handlers/sections.go, .planning/phases/02-authoritative-program-management/02-RESEARCH.md</read_first>
  <action>Refactor `backend/cmd/server/main.go` so normal server startup no longer deletes non-preset rooms or rewrites section rooms to a preset list. Keep safe bootstrap behavior limited to create-if-missing defaults needed for local/dev operation, but preserve admin-created rooms and room assignments across restarts. Do not reintroduce hidden schedule mutations through seed helpers.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/cmd/server/main.go does not contain `Where(\"name NOT IN ?\", presetRoomNames).Delete(&models.Room{})`
    - backend/cmd/server/main.go does not contain `Update(\"room\", targetRoom)`
    - backend/cmd/server/main.go still contains admin bootstrap creation or equivalent safe default bootstrap path
  </acceptance_criteria>
  <done>Admin-managed room and schedule data survive a normal backend restart.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && go test ./...` succeeds
- [ ] Authoritative program assignments exist as a separate backend model
- [ ] Admin program endpoints are reachable through router wiring
- [ ] Server startup no longer overwrites admin-managed room/schedule data
</verification>

<success_criteria>

- All tasks completed
- Backend exposes a durable authoritative program-management contract
- Restart behavior is safe for admin-managed rooms and schedule data
- Automated backend regression coverage exists for the new admin program API
</success_criteria>

<output>
After completion, create `.planning/phases/02-authoritative-program-management/02-01-SUMMARY.md`
</output>

