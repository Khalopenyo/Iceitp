---
phase: 02-authoritative-program-management
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - backend/internal/handlers/program_readmodel.go
  - backend/internal/handlers/program_readmodel_test.go
  - backend/internal/handlers/schedule.go
  - backend/internal/handlers/documents.go
autonomous: true
requirements:
  - PROG-01
  - PROG-03
user_setup: []
must_haves:
  truths:
    - Official program readers no longer build program output directly from `Profile.SectionID` and raw participant talk titles.
    - Backend read-models for admin schedule views and program documents consume authoritative approved assignments.
    - Personal and full program generation behave predictably when an authoritative assignment is missing.
  artifacts:
    - backend/internal/handlers/program_readmodel.go
    - backend/internal/handlers/schedule.go
    - backend/internal/handlers/documents.go
    - backend/internal/handlers/program_readmodel_test.go
  key_links:
    - Authoritative read-model helpers serve admin schedule grouping and PDF program generation.
    - Program PDFs reflect admin-approved assignment data rather than raw participant profile fields.
---

<objective>
Switch official program readers to the authoritative assignment source introduced in Phase 2.

Purpose: Organizer-controlled assignments do not matter unless admin schedule surfaces and generated program documents read from them instead of raw registration data.
Output: Shared authoritative read-model helpers plus updated schedule/document handlers and backend regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authoritative-program-management/02-RESEARCH.md
@.planning/phases/02-authoritative-program-management/02-01-PLAN.md
@backend/internal/handlers/schedule.go
@backend/internal/handlers/documents.go
@backend/internal/models/program_assignment.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a shared authoritative program read-model helper</name>
  <files>backend/internal/handlers/program_readmodel.go, backend/internal/handlers/schedule.go</files>
  <read_first>backend/internal/handlers/schedule.go, backend/internal/models/program_assignment.go, backend/internal/models/section.go, backend/internal/models/room.go, backend/internal/models/user.go</read_first>
  <action>Create `backend/internal/handlers/program_readmodel.go` with shared helper logic that batch-loads authoritative program assignments together with participant identity, approved section, and approved room data. Replace the current section-loop aggregation in `backend/internal/handlers/schedule.go` so admin schedule readers no longer rely on `profiles.section_id` as the official source. Keep the read-model additive where needed for later participant-facing Phase 3 work, but do not reintroduce raw-profile fallback for official admin program views.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/program_readmodel.go contains `ProgramAssignment`
    - backend/internal/handlers/schedule.go contains a call into the new read-model helper or equivalent shared authoritative loader
    - backend/internal/handlers/schedule.go does not contain a section loop that queries users by `profiles.section_id` for the official admin program reader
  </acceptance_criteria>
  <done>Authoritative schedule data is available from one shared backend read-model instead of duplicated raw-profile queries.</done>
</task>

<task type="auto">
  <name>Task 2: Generate official program documents from authoritative assignments</name>
  <files>backend/internal/handlers/documents.go, backend/internal/handlers/program_readmodel.go</files>
  <read_first>backend/internal/handlers/documents.go, backend/internal/handlers/program_readmodel.go, backend/internal/models/program_assignment.go, .planning/phases/02-authoritative-program-management/02-RESEARCH.md</read_first>
  <action>Update `backend/internal/handlers/documents.go` so `ProgramPDF` and its participant-loading helpers use authoritative program assignments rather than raw `Profile.SectionID` and `Profile.TalkTitle`. Personal-program mode should show a clear "official schedule not assigned" state when the participant has no approved assignment instead of silently falling back to raw profile inputs. Sort full-program output by approved schedule placement and keep section/room/talk-title text derived from the approved assignment source.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/documents.go contains `ProgramAssignment`
    - backend/internal/handlers/documents.go does not contain a `Where(\"section_id IN ?\", sectionIDs)` profile query as the official program source
    - backend/internal/handlers/documents.go contains text or logic equivalent to `official schedule not assigned`
  </acceptance_criteria>
  <done>Generated program PDFs are sourced from organizer-approved authoritative schedule data.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend regression tests for authoritative program readers</name>
  <files>backend/internal/handlers/program_readmodel_test.go</files>
  <read_first>backend/internal/handlers/program_readmodel.go, backend/internal/handlers/schedule.go, backend/internal/handlers/documents.go, backend/internal/handlers/program_test.go</read_first>
  <action>Create `backend/internal/handlers/program_readmodel_test.go` with focused coverage for the authoritative read-model behavior. Cover at least: admin schedule grouping uses approved assignments rather than raw profile section data, personal/full program loading prefers approved talk title and placement, and missing assignments produce the explicit unassigned behavior instead of raw-profile fallback for official program output.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/program_readmodel_test.go contains `func TestLoadProgramEntriesUsesAuthoritativeAssignments`
    - backend/internal/handlers/program_readmodel_test.go contains `func TestLoadProgramEntriesDoesNotFallbackToRawProfile`
    - `cd backend && go test ./...` exits 0
  </acceptance_criteria>
  <done>Official program readers are protected by regression coverage around authoritative-source selection.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && go test ./...` succeeds
- [ ] Admin schedule readers use authoritative assignments
- [ ] Program PDF generation uses authoritative assignments instead of raw profile fields
- [ ] Missing assignments produce explicit unassigned behavior for official program output
</verification>

<success_criteria>

- All tasks completed
- Official backend program readers use one authoritative schedule source
- Program documents no longer derive official schedule data from raw participant profile inputs
- Automated backend regression coverage exists for authoritative read-model behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-authoritative-program-management/02-03-SUMMARY.md`
</output>
