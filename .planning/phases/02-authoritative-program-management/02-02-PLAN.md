---
phase: 02-authoritative-program-management
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - frontend/src/pages/Admin.jsx
  - frontend/src/components/admin/AdminProgramTab.jsx
  - frontend/src/components/admin/AdminProgramList.jsx
  - frontend/src/components/admin/ProgramAssignmentForm.jsx
autonomous: true
requirements:
  - PROF-04
  - PROG-01
  - PROG-04
user_setup: []
must_haves:
  truths:
    - Admin has a dedicated UI surface for approving participant program data instead of only viewing read-only grouped schedule cards.
    - Organizer can edit approved attendance format, section, talk title, room, time slot, and external join link from the admin interface.
    - Existing admin sections, rooms, consents, conference settings, and tools remain accessible after the program-management UI is introduced.
  artifacts:
    - frontend/src/pages/Admin.jsx
    - frontend/src/components/admin/AdminProgramTab.jsx
    - frontend/src/components/admin/ProgramAssignmentForm.jsx
  key_links:
    - Admin program UI loads `/api/admin/program` and persists changes through `/api/admin/program/:userID`.
    - Organizer can compare participant-submitted fields against admin-approved fields in the same management flow.
---

<objective>
Build the organizer-facing UI for authoritative program management.

Purpose: Once the backend contract exists, organizers need an efficient surface to approve participant data and assign official room/time/link details.
Output: Admin program-management UI backed by the new authoritative program APIs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authoritative-program-management/02-RESEARCH.md
@.planning/phases/02-authoritative-program-management/02-01-PLAN.md
@frontend/src/pages/Admin.jsx
@frontend/src/pages/Register.jsx
@frontend/src/pages/Dashboard.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated admin program-management surface backed by the new API</name>
  <files>frontend/src/pages/Admin.jsx, frontend/src/components/admin/AdminProgramTab.jsx, frontend/src/components/admin/AdminProgramList.jsx</files>
  <read_first>frontend/src/pages/Admin.jsx, backend/internal/handlers/program.go, backend/internal/router/router.go, .planning/phases/02-authoritative-program-management/02-01-PLAN.md</read_first>
  <action>Refactor `frontend/src/pages/Admin.jsx` to introduce a dedicated program-management surface implemented through new child components such as `frontend/src/components/admin/AdminProgramTab.jsx` and `frontend/src/components/admin/AdminProgramList.jsx`. Load `/api/admin/program`, `/api/sections`, and `/api/rooms` there instead of relying on the existing read-only `/api/admin/schedule` cards. Show participant-submitted data and approved program data side by side so the organizer can see what changed.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Admin.jsx contains an import or render path for `AdminProgramTab`
    - frontend/src/components/admin/AdminProgramTab.jsx contains `apiGet(\"/admin/program\")`
    - frontend/src/components/admin/AdminProgramTab.jsx contains `apiGet(\"/sections\")`
    - frontend/src/components/admin/AdminProgramTab.jsx contains `apiGet(\"/rooms\")`
  </acceptance_criteria>
  <done>Admin has a dedicated authoritative program tab rather than only read-only grouped schedule cards.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the assignment editor for approved participant and schedule data</name>
  <files>frontend/src/components/admin/AdminProgramTab.jsx, frontend/src/components/admin/ProgramAssignmentForm.jsx</files>
  <read_first>frontend/src/components/admin/AdminProgramTab.jsx, backend/internal/models/program_assignment.go, backend/internal/handlers/program.go, frontend/src/lib/api.js</read_first>
  <action>Build a reusable assignment editor in `frontend/src/components/admin/ProgramAssignmentForm.jsx` and wire it into the program tab. The editor must support approved `user_type`, `section_id`, `talk_title`, `room_id`, `starts_at`, `ends_at`, and `join_url`, then persist through `apiPut(\"/admin/program/:userID\", payload)`. Keep join-link editing explicit and visible, with validation feedback from `err.message` surfaced in the UI when backend validation fails.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/components/admin/ProgramAssignmentForm.jsx contains `join_url`
    - frontend/src/components/admin/ProgramAssignmentForm.jsx contains `room_id`
    - frontend/src/components/admin/ProgramAssignmentForm.jsx contains `starts_at`
    - frontend/src/components/admin/AdminProgramTab.jsx contains `apiPut(`/admin/program/`
    - frontend/src/components/admin/AdminProgramTab.jsx contains `err.message`
  </acceptance_criteria>
  <done>Organizer can approve participant-owned and schedule-owned program data from one editor surface.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve existing admin flows while integrating the new program UI</name>
  <files>frontend/src/pages/Admin.jsx</files>
  <read_first>frontend/src/pages/Admin.jsx, frontend/src/components/admin/AdminProgramTab.jsx, frontend/src/components/admin/AdminProgramList.jsx</read_first>
  <action>Wire the new program-management tab into `frontend/src/pages/Admin.jsx` without breaking the existing users, sections, rooms, consents, conference settings, antiplagiat, and check-in tools. Keep existing load/refresh patterns functional after the refactor and ensure the admin page still builds cleanly even with the new child components introduced.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Admin.jsx still contains tabs for `users`, `sections`, `rooms`, `consents`, and `tools`
    - frontend/src/pages/Admin.jsx contains a program-management tab label or equivalent new tab branch
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>Authoritative program management is added without regressing unrelated admin tooling.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix frontend run build` succeeds
- [ ] Admin can load `/api/admin/program` data in the new UI
- [ ] Admin can edit approved participant and schedule fields from the new program surface
- [ ] Existing non-program admin tabs still render after the refactor
</verification>

<success_criteria>

- All tasks completed
- Admin UI supports authoritative participant/program approval and placement
- Organizer can edit join links and schedule slots without leaving the admin experience
- Existing admin flows remain intact after the program-management UI is added
</success_criteria>

<output>
After completion, create `.planning/phases/02-authoritative-program-management/02-02-SUMMARY.md`
</output>

