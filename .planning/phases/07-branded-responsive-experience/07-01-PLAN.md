---
phase: 07-branded-responsive-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Layout.jsx
  - frontend/src/pages/Welcome.jsx
  - frontend/src/index.css
autonomous: true
requirements:
  - INFO-01
  - INFO-02
user_setup: []
must_haves:
  truths:
    - Shared conference identity comes from existing conference settings instead of being duplicated in multiple frontend surfaces.
    - Public visitors can clearly find information about the university, the institute, and the conference from the main landing experience.
    - The branded app shell remains usable on smaller screens while preserving the current visual language.
  artifacts:
    - frontend/src/components/Layout.jsx
    - frontend/src/pages/Welcome.jsx
    - frontend/src/index.css
  key_links:
    - `GET /api/conference` becomes the frontend source for title, dates, description, and support email shown in the shell and landing.
    - Public landing content exposes explicit university / institute / conference information blocks without creating a separate CMS.
---

<objective>
Establish the shared conference identity and public-information structure for the final polish phase.

Purpose: The backend already stores authoritative conference metadata, but the frontend still hardcodes key event details in the shell and landing page. Phase 7 needs a single brand/data source before page-level responsive cleanup can be applied consistently.
Output: Shared conference metadata wiring in the app shell, explicit public information blocks on the landing surface, and responsive shell polish.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-branded-responsive-experience/07-RESEARCH.md
@backend/internal/handlers/conference.go
@frontend/src/components/Layout.jsx
@frontend/src/pages/Welcome.jsx
@frontend/src/pages/Admin.jsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load shared conference metadata into the frontend shell</name>
  <files>frontend/src/components/Layout.jsx</files>
  <read_first>frontend/src/components/Layout.jsx, backend/internal/handlers/conference.go, frontend/src/lib/api.js</read_first>
  <action>Refactor `frontend/src/components/Layout.jsx` so the header and footer consume conference metadata from the existing public `GET /api/conference` contract instead of relying on hardcoded title, empty subtitle, and fixed support email. Keep auth/session logic intact and prefer a lightweight local load path over introducing a large global state layer.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/components/Layout.jsx contains a `GET /conference` load path or equivalent shared conference-data helper usage
    - header or footer branding reads conference title and support/contact details from loaded conference metadata
    - unauthenticated and authenticated navigation behavior remains intact
  </acceptance_criteria>
  <done>The app shell now renders shared conference identity from one frontend data source.</done>
</task>

<task type="auto">
  <name>Task 2: Make university, institute, and conference information explicit on the landing page</name>
  <files>frontend/src/pages/Welcome.jsx</files>
  <read_first>frontend/src/pages/Welcome.jsx, .planning/phases/07-branded-responsive-experience/07-RESEARCH.md, backend/internal/models/conference.go</read_first>
  <action>Update `frontend/src/pages/Welcome.jsx` so the visitor-facing experience explicitly exposes information about the university, the institute, and the conference while preserving the current branded landing-page direction. Reuse shared conference metadata where it fits naturally for title, date, description, or support context, but avoid flattening the existing page into a generic brochure layout.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Welcome.jsx contains explicit university / institute / conference sections or equivalent public-information blocks
    - landing copy uses conference metadata from the shared source where appropriate instead of duplicating all key event details locally
    - the primary CTA to registration or dashboard is preserved
  </acceptance_criteria>
  <done>Visitors can find required public information from the main landing surface without losing the existing conference branding.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten responsive shell behavior for public and authenticated navigation</name>
  <files>frontend/src/index.css</files>
  <read_first>frontend/src/index.css, frontend/src/components/Layout.jsx, frontend/src/pages/Welcome.jsx</read_first>
  <action>Extend `frontend/src/index.css` so the shared shell behaves cleanly on phone and tablet widths: header branding, navigation, auth actions, footer links, and landing-section entry patterns should wrap or stack intentionally rather than relying on accidental overflow or cramped wrapping. Preserve the existing visual language instead of redesigning the palette or typography system in this phase.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/index.css contains explicit responsive treatment for header, nav, auth actions, and footer beyond the current basic wrap behavior
    - landing shell spacing remains usable at phone widths
    - no new desktop-only shell assumptions are introduced
  </acceptance_criteria>
  <done>The branded app shell is ready for narrower screens before deeper page-level cleanup begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix frontend run build` succeeds
- [ ] Shared conference metadata appears in shell-level UI instead of only in admin settings
- [ ] Visitor can find university, institute, and conference information from the public landing experience
- [ ] Header and footer remain usable on phone and tablet widths
</verification>

<success_criteria>

- All tasks completed
- Public and authenticated shell surfaces share one conference identity source
- Public information coverage satisfies the required university / institute / conference presentation
- The shell is ready for participant-flow responsive cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/07-branded-responsive-experience/07-01-SUMMARY.md`
</output>
