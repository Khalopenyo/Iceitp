---
phase: 05-conference-communication
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - frontend/src/pages/Chat.jsx
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
user_setup: []
must_haves:
  truths:
    - The existing chat page keeps text messaging, channel switching, search, and edit/delete behavior while adding attachment upload UX.
    - Participants can attach allowed files before sending a message and can download attachment rows from message history afterward.
    - The UI stays compatible with both conference and section chat scopes.
  artifacts:
    - frontend/src/pages/Chat.jsx
  key_links:
    - Chat composer submits message content plus selected files against the new backend contract.
    - Attachment metadata returned by `GET /api/chat` is rendered in message history with explicit download actions.
---

<objective>
Add attachment UX to the existing participant chat page.

Purpose: Backend file support is not useful until participants can pick attachments, submit them from the current chat composer, and download them back from the chat history.
Output: Chat attachment composer, selected-file queue, attachment rendering, and preserved text-chat ergonomics.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-conference-communication/05-RESEARCH.md
@.planning/phases/05-conference-communication/05-01-PLAN.md
@frontend/src/pages/Chat.jsx
@frontend/src/lib/api.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the chat composer for file selection and multipart submission</name>
  <files>frontend/src/pages/Chat.jsx</files>
  <read_first>frontend/src/pages/Chat.jsx, frontend/src/lib/api.js, .planning/phases/05-conference-communication/05-01-PLAN.md</read_first>
  <action>Update `frontend/src/pages/Chat.jsx` so the existing composer can select one or more files, show the selected-file queue before send, remove selected files before submission, and send the payload with `FormData` against the new backend contract. Preserve the text-only send path and existing draft behavior when no files are selected.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Chat.jsx contains a file input with `multiple` or equivalent multi-file support
    - frontend/src/pages/Chat.jsx contains `FormData` or `apiPostForm` usage for message submission
    - frontend/src/pages/Chat.jsx contains selected-file queue or remove-before-send handling
  </acceptance_criteria>
  <done>Participants can attach files directly from the current chat composer without losing text-only messaging.</done>
</task>

<task type="auto">
  <name>Task 2: Render attachment rows and download actions in message history</name>
  <files>frontend/src/pages/Chat.jsx</files>
  <read_first>frontend/src/pages/Chat.jsx, backend/internal/handlers/chat.go</read_first>
  <action>Render attachment metadata returned by the backend underneath each message bubble, including clear file names and download actions. Make sure the rendering works for both conference and section channels and does not regress search, unread tracking, or text editing behavior.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Chat.jsx renders attachment metadata from message payloads
    - frontend/src/pages/Chat.jsx contains an explicit download action for attachments
    - attachment UI is conditionally hidden when a message has no files
  </acceptance_criteria>
  <done>Attachment-bearing messages become visible and actionable inside the chat history.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve existing chat ergonomics while integrating attachments</name>
  <files>frontend/src/pages/Chat.jsx</files>
  <read_first>frontend/src/pages/Chat.jsx</read_first>
  <action>Keep the existing channel switching, drafts, search, edit/delete, and text-only composition experience intact while integrating the new attachment controls. Avoid introducing a dependency on new global styling files unless strictly necessary, and keep the page build-clean.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Chat.jsx still contains the current channel-switching and edit/delete flows
    - frontend/src/pages/Chat.jsx supports sending a text-only message without attachments
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>The chat page gains attachments without sacrificing the existing interaction quality.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix frontend run build` succeeds
- [ ] Participant can queue and remove files before sending a chat message
- [ ] Messages with attachments render download actions in history
- [ ] Text-only chat flow still works unchanged
</verification>

<success_criteria>

- All tasks completed
- Chat UI supports attachment upload and download in existing scopes
- Existing chat ergonomics remain intact
- Frontend build stays green after the attachment integration
</success_criteria>

<output>
After completion, create `.planning/phases/05-conference-communication/05-02-SUMMARY.md`
</output>
