---
phase: 05-conference-communication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/chat.go
  - backend/internal/models/chat_attachment.go
  - backend/internal/db/db.go
  - backend/internal/handlers/chat.go
  - backend/internal/router/router.go
  - backend/internal/handlers/chat_test.go
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
user_setup: []
must_haves:
  truths:
    - The backend preserves existing text-chat behavior while adding durable attachment metadata and file storage for chat messages.
    - Attachments are validated against explicit allowlist and size rules before they are saved.
    - Attachment downloads are served through authenticated scope-aware authorization rather than public static paths.
  artifacts:
    - backend/internal/models/chat_attachment.go
    - backend/internal/handlers/chat.go
    - backend/internal/router/router.go
    - backend/internal/handlers/chat_test.go
  key_links:
    - `POST /api/chat` accepts message content plus optional attachments without breaking text-only posting.
    - `GET /api/chat` returns attachment metadata in message payloads so the frontend can render file rows.
    - `GET /api/chat/attachments/:id` only serves files the current user is authorized to access.
---

<objective>
Establish the backend attachment contract for conference communication.

Purpose: The current chat is text-only, so Phase 5 cannot deliver file exchange until the backend can store attachment metadata, validate uploads, and authorize downloads by chat scope.
Output: Attachment model, upload/download backend behavior, router wiring, and regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-conference-communication/05-RESEARCH.md
@backend/internal/models/chat.go
@backend/internal/handlers/chat.go
@backend/internal/router/router.go
@backend/internal/handlers/submissions.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chat attachment persistence and migration support</name>
  <files>backend/internal/models/chat.go, backend/internal/models/chat_attachment.go, backend/internal/db/db.go</files>
  <read_first>backend/internal/models/chat.go, backend/internal/db/db.go, backend/internal/handlers/submissions.go, .planning/phases/05-conference-communication/05-RESEARCH.md</read_first>
  <action>Create a dedicated `ChatAttachment` model in `backend/internal/models/chat_attachment.go` linked to `ChatMessage`, with fields for attachment metadata such as original file name, stored file path or stored name, content type, file size, and timestamps. Extend `backend/internal/models/chat.go` as needed so list responses can include attachments cleanly, and add the new model to `backend/internal/db/db.go` migrations.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/models/chat_attachment.go contains `type ChatAttachment struct`
    - backend/internal/models/chat_attachment.go contains `MessageID`
    - backend/internal/models/chat_attachment.go contains `FilePath` or `StoredName`
    - backend/internal/db/db.go contains `&models.ChatAttachment{}`
  </acceptance_criteria>
  <done>Chat messages can now own durable attachment metadata instead of being text-only rows.</done>
</task>

<task type="auto">
  <name>Task 2: Implement scoped attachment upload, list, download, and cleanup behavior</name>
  <files>backend/internal/handlers/chat.go, backend/internal/router/router.go</files>
  <read_first>backend/internal/handlers/chat.go, backend/internal/router/router.go, backend/internal/models/chat_attachment.go, backend/internal/handlers/submissions.go</read_first>
  <action>Extend `backend/internal/handlers/chat.go` so chat creation supports optional multipart attachments, validates an explicit allowlist and size limit, stores files under controlled chat storage, returns attachment metadata from `GET /api/chat`, and serves downloads through an authenticated endpoint such as `GET /api/chat/attachments/:id`. Preserve text-only chat posting, and ensure deleting a message also removes or invalidates its stored attachments. Wire the new download route in `backend/internal/router/router.go`.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/chat.go contains `FormFile`, `MultipartForm`, or equivalent multipart handling
    - backend/internal/router/router.go contains `/chat/attachments/:id`
    - backend/internal/handlers/chat.go contains explicit attachment validation logic for size and allowed types
    - backend/internal/handlers/chat.go returns attachment metadata in chat list responses
  </acceptance_criteria>
  <done>Participants can upload allowed files and retrieve them later through a scope-aware backend contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add chat backend regression tests for text preservation, upload validation, and authorized downloads</name>
  <files>backend/internal/handlers/chat_test.go</files>
  <read_first>backend/internal/handlers/chat.go, backend/internal/models/chat_attachment.go, backend/internal/handlers/auth_test.go</read_first>
  <action>Create `backend/internal/handlers/chat_test.go` with endpoint-level coverage for at least: text-only chat still works, allowed attachments can be uploaded and listed, unauthorized section download is rejected, and invalid attachment types or oversize uploads are rejected. Keep tests focused on the backend contract rather than frontend behavior.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/chat_test.go contains `func TestPostChatMessageWithAttachment`
    - backend/internal/handlers/chat_test.go contains `func TestChatAttachmentDownloadRequiresAuthorizedScope`
    - backend/internal/handlers/chat_test.go contains `func TestPostChatMessageRejectsInvalidAttachment`
    - `cd backend && go test ./...` exits 0
  </acceptance_criteria>
  <done>The attachment backend contract is protected against the main regression and authorization risks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && go test ./...` succeeds
- [ ] Chat messages still work without attachments
- [ ] Allowed attachments can be uploaded and appear in chat list responses
- [ ] Attachment downloads are gated by authenticated scope checks
</verification>

<success_criteria>

- All tasks completed
- Backend supports safe scoped file exchange in chat
- Existing text chat remains intact
- Automated backend coverage exists for upload and download authorization behavior
</success_criteria>

<output>
After completion, create `.planning/phases/05-conference-communication/05-01-SUMMARY.md`
</output>
