---
phase: 05-conference-communication
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/internal/handlers/feedback.go
  - backend/internal/handlers/feedback_test.go
  - frontend/src/pages/Feedback.jsx
  - frontend/src/pages/Admin.jsx
autonomous: true
requirements:
  - FEED-01
user_setup: []
must_haves:
  truths:
    - Participants can submit usable conference feedback and improvement suggestions through a clearer feedback flow than the current alert-only form.
    - Organizers can review submitted feedback inside the admin interface without leaving the product.
    - Feedback backend behavior is validated and covered by regression tests.
  artifacts:
    - backend/internal/handlers/feedback.go
    - backend/internal/handlers/feedback_test.go
    - frontend/src/pages/Feedback.jsx
    - frontend/src/pages/Admin.jsx
  key_links:
    - `POST /api/feedback` accepts validated participant feedback and returns a stable response contract.
    - `GET /api/admin/feedback` returns organizer-usable feedback entries with author context for admin review UI.
---

<objective>
Complete the feedback loop for participants and organizers.

Purpose: A bare feedback form is not enough for the conference workflow until participants get a clear submission UX and organizers can actually review suggestions inside the admin panel.
Output: Improved feedback backend contract, participant feedback UX, admin feedback tab, and regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-conference-communication/05-RESEARCH.md
@backend/internal/handlers/feedback.go
@frontend/src/pages/Feedback.jsx
@frontend/src/pages/Admin.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten feedback backend validation and add organizer-readable list responses</name>
  <files>backend/internal/handlers/feedback.go, backend/internal/handlers/feedback_test.go</files>
  <read_first>backend/internal/handlers/feedback.go, backend/internal/models/feedback.go, backend/internal/handlers/chat.go, .planning/phases/05-conference-communication/05-RESEARCH.md</read_first>
  <action>Refine `backend/internal/handlers/feedback.go` so participant submissions trim and validate suggestion content cleanly, and organizer listing returns a response shape with enough author/time context for admin UI rendering. Add `backend/internal/handlers/feedback_test.go` to cover valid submission, invalid payload rejection, and organizer list loading.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/feedback.go contains explicit validation beyond the current minimal rating check
    - backend/internal/handlers/feedback_test.go contains `func TestCreateFeedback`
    - backend/internal/handlers/feedback_test.go contains organizer list coverage
    - `cd backend && go test ./...` exits 0
  </acceptance_criteria>
  <done>The feedback backend becomes stable enough for both participant submission and organizer review.</done>
</task>

<task type="auto">
  <name>Task 2: Improve participant feedback UX with clear suggestion framing and inline success state</name>
  <files>frontend/src/pages/Feedback.jsx</files>
  <read_first>frontend/src/pages/Feedback.jsx, backend/internal/handlers/feedback.go</read_first>
  <action>Update `frontend/src/pages/Feedback.jsx` so the participant gets a clearer form for conference review and improvement suggestions, with inline success/error feedback instead of alert-only handling. Keep the flow simple and compatible with the existing authenticated app shell.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Feedback.jsx contains inline success or error state rendering
    - frontend/src/pages/Feedback.jsx explicitly frames the textarea as conference feedback or improvement suggestions
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>Participants can submit useful feedback without relying on modal alerts.</done>
</task>

<task type="auto">
  <name>Task 3: Add an admin feedback review tab without regressing existing admin surfaces</name>
  <files>frontend/src/pages/Admin.jsx</files>
  <read_first>frontend/src/pages/Admin.jsx, backend/internal/handlers/feedback.go</read_first>
  <action>Add a dedicated feedback tab or equivalent organizer surface in `frontend/src/pages/Admin.jsx` that loads `GET /api/admin/feedback` and shows submitted feedback entries with readable participant and time context. Keep the existing admin tabs working and avoid disrupting unrelated admin tools.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Admin.jsx contains a feedback admin tab or equivalent surface
    - frontend/src/pages/Admin.jsx loads admin feedback data
    - existing admin tabs remain intact and build-clean
  </acceptance_criteria>
  <done>Organizers can finally review participant suggestions inside the platform.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && go test ./...` succeeds
- [ ] `npm --prefix frontend run build` succeeds
- [ ] Participant can submit feedback with inline confirmation
- [ ] Admin can review submitted feedback inside the admin UI
</verification>

<success_criteria>

- All tasks completed
- Feedback submission is usable for participants
- Feedback review is usable for organizers
- Automated backend coverage exists for feedback create/list behavior
</success_criteria>

<output>
After completion, create `.planning/phases/05-conference-communication/05-03-SUMMARY.md`
</output>
