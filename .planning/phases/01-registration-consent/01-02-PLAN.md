---
phase: 01-registration-consent
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - frontend/src/pages/Register.jsx
  - frontend/src/pages/Login.jsx
  - frontend/src/pages/PersonalData.jsx
  - frontend/src/pages/ConsentAuthors.jsx
autonomous: true
requirements:
  - CONS-01
  - AUTH-01
  - AUTH-02
  - PROF-01
  - PROF-02
  - PROF-03
user_setup: []
must_haves:
  truths:
    - Registration UI shows the required consent information before account creation completes.
    - Registration UI collects attendance format, participant profile fields, section choice, and talk title using the backend contract from plan 01.
    - Login and post-registration flows still persist the auth token and return the participant to the authenticated product.
  artifacts:
    - frontend/src/pages/Register.jsx
    - frontend/src/pages/PersonalData.jsx
    - frontend/src/pages/ConsentAuthors.jsx
    - frontend/src/pages/Login.jsx
  key_links:
    - Register.jsx sends the exact consent field names expected by POST /api/auth/register.
    - PersonalData.jsx and ConsentAuthors.jsx expose the legal copy linked from the registration form.
---

<objective>
Bring the registration and legal-information UI in line with the phase-1 backend contract.

Purpose: Users need a clear, legally explicit onboarding flow that captures all required participant data without leaking later program-management assumptions into registration.
Output: Updated registration UI, real legal content pages, and stable auth continuity after registration and login.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-registration-consent/01-RESEARCH.md
@.planning/phases/01-registration-consent/01-01-PLAN.md
@frontend/src/pages/Register.jsx
@frontend/src/pages/Login.jsx
@frontend/src/pages/PersonalData.jsx
@frontend/src/pages/ConsentAuthors.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the registration form to use the explicit phase-1 payload contract</name>
  <files>frontend/src/pages/Register.jsx</files>
  <read_first>frontend/src/pages/Register.jsx, backend/internal/handlers/auth.go, .planning/phases/01-registration-consent/01-01-PLAN.md</read_first>
  <action>Refactor `frontend/src/pages/Register.jsx` so the form state and request payload use `consent_personal_data`, `consent_publication`, and `consent_version: "registration-consent-v1"` instead of the current single `consent` flag. Keep the existing multi-step structure, continue collecting `user_type`, participant identity fields, `section_id`, `talk_title`, and `phone`, and remove the step-advance guard that currently requires `selectedSection.room`. The form should require a section and talk title, but not require an already-assigned room.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Register.jsx contains `consent_personal_data`
    - frontend/src/pages/Register.jsx contains `consent_publication`
    - frontend/src/pages/Register.jsx contains `consent_version`
    - frontend/src/pages/Register.jsx does not contain `selectedSection && selectedSection.room`
  </acceptance_criteria>
  <done>The registration page submits the exact backend payload for versioned consent and no longer blocks on room assignment.</done>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder legal pages with registration-linked consent content</name>
  <files>frontend/src/pages/PersonalData.jsx, frontend/src/pages/ConsentAuthors.jsx</files>
  <read_first>frontend/src/pages/PersonalData.jsx, frontend/src/pages/ConsentAuthors.jsx, .planning/phases/01-registration-consent/01-RESEARCH.md</read_first>
  <action>Replace the draft text in `frontend/src/pages/PersonalData.jsx` with formal operational personal-data processing content for conference registration, storage, and participant servicing. Keep `frontend/src/pages/ConsentAuthors.jsx` as the publication/distribution consent page, but align its wording and headings so it is clearly the publication-facing counterpart to the operational policy. Ensure the registration form references both legal surfaces in user-visible copy.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/PersonalData.jsx does not contain `Черновик`
    - frontend/src/pages/PersonalData.jsx contains `персональных данных`
    - frontend/src/pages/ConsentAuthors.jsx contains `публикац`
    - frontend/src/pages/Register.jsx contains `/personal-data` and `/consent-authors`
  </acceptance_criteria>
  <done>The registration flow links to non-placeholder legal text for both processing and publication consent.</done>
</task>

<task type="auto">
  <name>Task 3: Keep login and registration feedback aligned with the updated auth flow</name>
  <files>frontend/src/pages/Register.jsx, frontend/src/pages/Login.jsx</files>
  <read_first>frontend/src/pages/Register.jsx, frontend/src/pages/Login.jsx, frontend/src/lib/auth.js, frontend/src/lib/api.js</read_first>
  <action>Update `frontend/src/pages/Register.jsx` and `frontend/src/pages/Login.jsx` to surface backend error text through `err.message` when available instead of using only generic alert strings, while preserving the current token storage via `setToken(...)` and navigation to authenticated routes. Do not add forgot-password UI in this phase; keep the login screen focused on existing access flow continuity.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Register.jsx contains `err.message`
    - frontend/src/pages/Login.jsx contains `err.message`
    - frontend/src/pages/Register.jsx contains `setToken(data.token)`
    - frontend/src/pages/Login.jsx contains `setToken(data.token)`
  </acceptance_criteria>
  <done>Authentication screens preserve session continuity and expose backend validation failures more precisely.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix frontend run build` succeeds
- [ ] Register page sends explicit consent fields and no longer gates on section room
- [ ] Legal pages contain non-placeholder consent text
</verification>

<success_criteria>

- All tasks completed
- Registration UI mirrors the backend phase-1 contract
- Legal content linked from registration is explicit and non-placeholder
- Login and registration still produce a working authenticated session after success
</success_criteria>

<output>
After completion, create `.planning/phases/01-registration-consent/01-02-SUMMARY.md`
</output>
