---
phase: 01-registration-consent
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - frontend/src/pages/Dashboard.jsx
autonomous: true
requirements:
  - AUTH-02
  - PROF-01
  - PROF-02
  - PROF-03
user_setup: []
must_haves:
  truths:
    - Authenticated participant can edit the phase-1 conference profile fields after registration, including section choice and talk title.
    - Profile changes persist through the existing authenticated API and stay visible after the session is reloaded.
    - Dashboard editing uses the same field contract as registration for participant-owned data.
  artifacts:
    - frontend/src/pages/Dashboard.jsx
  key_links:
    - Dashboard profile save uses PUT /api/me/profile with section and talk-title fields aligned to the phase-1 contract.
    - Successful profile save refreshes or updates cached authenticated user state used by the app shell.
---

<objective>
Align the authenticated participant profile experience with the phase-1 registration contract.

Purpose: Registration alone is not enough; participants also need a stable post-registration place to maintain their own conference data while staying inside an authenticated session.
Output: Updated dashboard profile editor and session-cache continuity for participant-owned conference fields.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-registration-consent/01-RESEARCH.md
@.planning/phases/01-registration-consent/01-01-PLAN.md
@frontend/src/pages/Dashboard.jsx
@frontend/src/pages/Register.jsx
@frontend/src/lib/auth.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the dashboard profile editor to cover phase-1 conference fields</name>
  <files>frontend/src/pages/Dashboard.jsx</files>
  <read_first>frontend/src/pages/Dashboard.jsx, frontend/src/pages/Register.jsx, backend/internal/handlers/users.go</read_first>
  <action>Update the profile tab in `frontend/src/pages/Dashboard.jsx` so it loads the available sections from `/api/sections`, renders a section selector alongside the existing editable participant profile fields, and keeps `talk_title`, `phone`, `organization`, `position`, `city`, and `degree` aligned with the registration field names. Use the same `section_id` key shape expected by `PUT /api/me/profile` instead of introducing a second dashboard-only contract.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Dashboard.jsx contains `apiGet("/sections")`
    - frontend/src/pages/Dashboard.jsx contains `section_id`
    - frontend/src/pages/Dashboard.jsx contains `talk_title`
    - frontend/src/pages/Dashboard.jsx contains a `<select` element or select-rendering branch for section choice
  </acceptance_criteria>
  <done>The dashboard profile editor lets participants maintain section and talk metadata using the same field contract as registration.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh cached authenticated user state after profile updates</name>
  <files>frontend/src/pages/Dashboard.jsx</files>
  <read_first>frontend/src/pages/Dashboard.jsx, frontend/src/lib/auth.js, frontend/src/components/Layout.jsx, frontend/src/lib/api.js</read_first>
  <action>After a successful `PUT /api/me/profile` in `frontend/src/pages/Dashboard.jsx`, fetch the fresh `/api/me` payload and call `setUser(...)` from `frontend/src/lib/auth.js` so the cached authenticated user state stays consistent with profile edits across navigation and reloads. Keep the existing token-based auth flow intact; do not redesign storage in this phase.</action>
  <verify>npm --prefix frontend run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/Dashboard.jsx contains `setUser(`
    - frontend/src/pages/Dashboard.jsx contains `apiGet("/me")`
    - frontend/src/pages/Dashboard.jsx contains `apiPut("/me/profile", profile)`
    - npm --prefix frontend run build exits 0
  </acceptance_criteria>
  <done>Profile edits remain visible in the authenticated shell after save and subsequent page loads.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm --prefix frontend run build` succeeds
- [ ] Dashboard can edit section and talk-title fields with the phase-1 contract
- [ ] Cached user state refreshes after profile save
</verification>

<success_criteria>

- All tasks completed
- Participant can maintain phase-1 profile fields after registration
- Session continuity remains intact after profile edits
- Dashboard editing does not invent a second contract for section and talk-title data
</success_criteria>

<output>
After completion, create `.planning/phases/01-registration-consent/01-03-SUMMARY.md`
</output>
