---
phase: 01-registration-consent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/internal/models/consent.go
  - backend/internal/models/user.go
  - backend/internal/handlers/auth.go
  - backend/internal/handlers/users.go
  - backend/internal/db/db.go
  - backend/internal/handlers/auth_test.go
autonomous: true
requirements:
  - CONS-02
  - AUTH-01
  - AUTH-02
  - PROF-01
  - PROF-02
  - PROF-03
user_setup: []
must_haves:
  truths:
    - Registration API accepts attendance format, participant profile fields, section choice, talk title, and explicit consent values without requiring a room assignment on the selected section.
    - Consent records persist stable type and version metadata for the registration flow.
    - Profile update rules for section and talk-title fields match the registration contract.
  artifacts:
    - backend/internal/handlers/auth.go
    - backend/internal/models/consent.go
    - backend/internal/handlers/users.go
    - backend/internal/handlers/auth_test.go
  key_links:
    - POST /api/auth/register creates User and Profile data and writes consent audit records using the same consent-version contract.
    - PUT /api/me/profile validates section IDs against the same section source used by registration.
---

<objective>
Stabilize the backend contract for registration and participant profile data.

Purpose: Phase 1 needs a reliable API contract before the registration UI and dashboard profile editor can be aligned.
Output: Updated auth/profile persistence and handler behavior, plus backend regression coverage for registration and consent.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-registration-consent/01-RESEARCH.md
@backend/internal/handlers/auth.go
@backend/internal/handlers/users.go
@backend/internal/models/user.go
@backend/internal/models/consent.go
@backend/internal/db/db.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the registration consent contract and consent audit model</name>
  <files>backend/internal/models/consent.go, backend/internal/models/user.go, backend/internal/handlers/auth.go, backend/internal/db/db.go</files>
  <read_first>backend/internal/models/consent.go, backend/internal/models/user.go, backend/internal/handlers/auth.go, backend/internal/db/db.go, .planning/phases/01-registration-consent/01-RESEARCH.md</read_first>
  <action>Add explicit registration consent fields `ConsentPersonalData bool`, `ConsentPublication bool`, and `ConsentVersion string` to the registration request contract in `backend/internal/handlers/auth.go` with JSON keys `consent_personal_data`, `consent_publication`, and `consent_version`. In `backend/internal/models/consent.go`, add stable consent type constants `ConsentTypePersonalData` and `ConsentTypePublication` and keep `ConsentVersion` persisted per record. Preserve `Profile.ConsentGiven` in `backend/internal/models/user.go` as a compatibility field computed from the required consents instead of treating it as the only source of truth. Ensure `backend/internal/db/db.go` still automigrates the updated consent model.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/auth.go contains `json:"consent_personal_data"` and `json:"consent_publication"`
    - backend/internal/handlers/auth.go contains `ConsentVersion string`
    - backend/internal/models/consent.go contains `ConsentTypePersonalData` and `ConsentTypePublication`
    - backend/internal/db/db.go contains `&models.ConsentLog{}`
  </acceptance_criteria>
  <done>Registration has an explicit versioned consent contract and the consent model exposes stable consent-type identifiers.</done>
</task>

<task type="auto">
  <name>Task 2: Align registration and profile-update validation with phase-1 scope</name>
  <files>backend/internal/handlers/auth.go, backend/internal/handlers/users.go</files>
  <read_first>backend/internal/handlers/auth.go, backend/internal/handlers/users.go, backend/internal/models/section.go, backend/internal/handlers/sections.go, .planning/phases/01-registration-consent/01-RESEARCH.md</read_first>
  <action>Update `Register` in `backend/internal/handlers/auth.go` to trim and validate required identity fields, require a valid `section_id`, require `talk_title`, and remove the current section-room guard so registration no longer fails when `section.Room` is empty. Update `UpdateProfile` in `backend/internal/handlers/users.go` to validate `section_id` when provided, persist `full_name`, `organization`, `position`, `city`, `degree`, `phone`, `section_id`, and `talk_title` as the participant-editable Phase 1 profile contract, and reject invalid section references with the same `selected section not found` error wording used by registration.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/auth.go does not contain `selected section has no assigned room`
    - backend/internal/handlers/auth.go contains `selected section not found`
    - backend/internal/handlers/users.go contains `selected section not found`
    - backend/internal/handlers/users.go contains assignments for `SectionID` and `TalkTitle`
  </acceptance_criteria>
  <done>Registration and profile update use the same phase-1 rules for section choice and participant conference fields.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend regression coverage for registration and consent behavior</name>
  <files>backend/internal/handlers/auth_test.go</files>
  <read_first>backend/internal/handlers/auth.go, backend/internal/handlers/users.go, backend/internal/models/consent.go, backend/internal/db/db.go</read_first>
  <action>Create `backend/internal/handlers/auth_test.go` with table-driven or focused handler tests that cover at least: successful registration with a valid section that has an empty `Room`, registration rejection when `consent_personal_data` or `consent_publication` is false, and consent-log creation using the configured `consent_version`. Use the current handler behavior and models rather than adding a separate test-only API contract.</action>
  <verify>cd backend && go test ./...</verify>
  <acceptance_criteria>
    - backend/internal/handlers/auth_test.go contains `func TestRegisterAllowsSectionWithoutRoom`
    - backend/internal/handlers/auth_test.go contains `func TestRegisterRequiresExplicitConsents`
    - backend/internal/handlers/auth_test.go contains `func TestRegisterLogsConsentVersion`
    - `cd backend && go test ./...` exits 0
  </acceptance_criteria>
  <done>Backend registration and consent rules are protected by automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && go test ./...` succeeds
- [ ] Registration no longer depends on `section.Room`
- [ ] Consent audit records use explicit type/version fields
</verification>

<success_criteria>

- All tasks completed
- Backend registration and profile handlers use one consistent phase-1 participant contract
- Registration writes versioned consent audit records
- Automated backend regression coverage exists for the new registration rules
</success_criteria>

<output>
After completion, create `.planning/phases/01-registration-consent/01-01-SUMMARY.md`
</output>
